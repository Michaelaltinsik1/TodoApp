export interface todoItemsType {
  // todoList: string[];
  todoList: { item: string; id: string }[];
}

export interface todoItemType {
  // todoList: string[];
  todoList: { item: string; id: string };
}
